workflow:
  nodes:
    start:
      label: start
      kind: local
      host: local
      exec: echo start
      name: start
    mlp_mnist:
      label: mlp_mnist
      kind: local
      host: local
      exec: echo mnist
      name: mlp_mnist
    end:
      label: stop
      kind: local
      host: local
      exec: echo end
      name: end
  dependencies: {}
